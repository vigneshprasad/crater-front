import { motion } from "framer-motion";
import styled from "styled-components";

import { Box } from "../System";

// eslint-disable-next-line import/prefer-default-export
export const AnimatedBox = styled(motion(Box))``;
